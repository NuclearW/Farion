# Farion config file
Bot:
    DCCPassword: "changeme"
    Nick: "Chatbot"
    NickServ: "woowoo"
    RetryConnect: true
Server:
    Hostname: irc.esper.net
    Port: 6667
    SSL: false
Channel:
    Name: "#channel"
ModChannel:
    Name: "#channel"
RemoteConsoleUsers:
    - "Username"
Message:
    IrcMessage: "<{username}> {message}"
    IrcMeMessage: "* {username} {message}"
    GameMessage: "&f[IRC] <{nickname}> {message}"
    GameMeMessage: "&f[IRC] * {nickname} {message}"